/**
 * Automatically generated by QHGC tool
 * @date: {{time}}
 */
import React from "react";
import {Button, Form, Input, Space} from "antd";
import "@/common/qh4-form.less";

const layout = {
  labelCol: {span: 8},
  wrapperCol: {span: 8},
};
const noLabelLayout = {
  wrapperCol: {offset: 8, span: 8},
};

/**
 * 新建和更新通用的表单
 * @param props
 * @returns {JSX.Element}
 * @constructor
 */
const EditForm = (props) => {
  return (
    <Form
      className="qh4-basic-form"
      {...layout}
      form={props.form}
      onFinish={props.onFinish}
    >
      {{form_item}}
      <Form.Item {...noLabelLayout}>
        <Space size="middle">
          <Button type="primary" htmlType="submit">
            提交
          </Button>
          <Button htmlType="button" onClick={() => props.form.resetFields()}>
            重置
          </Button>
        </Space>
      </Form.Item>
    </Form>
  )
}

export default EditForm;
